function friend4letters(friends) {
  const friend = ["Ryan", "Kieran", "Mark"];
  friends.forEach(function (name) {
    if (name.lenght != 4) {
      friends.filter(name);
    }
  });
  return friends;
}
